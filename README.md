# Test Deploy App

Simple Express app to test DeployIt push-to-deploy.

## Endpoints

- `GET /` — Hello message
- `GET /api/status` — Server uptime
- `GET /api/hello?name=Prachi` — Personalized greeting

## Local dev

```bash
npm install
npm run dev
```
